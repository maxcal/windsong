<h3><%= Station.model_name.human(count: 2).capitalize  %></h3>
<% if @stations.any? %>
<ul>
  <% @stations.each do |s| %>
  <li><%= link_to(s.name, station_path(s.to_param)) %></li>
  <% end %>
</ul>
<% end %>